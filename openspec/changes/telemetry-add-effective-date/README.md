# telemetry-add-effective-date

Add EffectiveDate (= CapturedAt - 1 day) to telemetry.Snapshot so the history graph shows the calendar day the nightly snapshot represents, not the morning it was read.
