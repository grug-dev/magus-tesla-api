## 1. Interface + sqlc query (independent waves — disjoint files)

- [x] 1.1 Add `SnapshotsByVehicleBetween(ctx context.Context, accountID uuid.UUID, teslaID int64, start, end time.Time) ([]Snapshot, error)` to the `Reader` interface in `internal/telemetry/telemetry.go`. Doc comment states: returns snapshots whose **`EffectiveDate` calendar day** falls in `[start, end]` inclusive, ordered **ascending by `EffectiveDate`** (== ascending by `captured_at`); `start`/`end` are whole UTC-midnight-bounded calendar days; `end` is inclusive; the port is a clean bounded window with NO lookback parameter (lookback is a gateway concern — Decision #4); returns an empty (non-nil) slice on no data; reuses the single `rowToSnapshot` mapper; additive alongside `SnapshotsByVehicleSince` (kept unchanged — Decision #4). Place it adjacent to `SnapshotsByVehicleSince` in the interface and in the doc body of `internal/telemetry/AGENTS.md`'s "Public interface (the port)" Reader section. Depends on: nothing (first wave, can run in parallel with 1.2).
- [x] 1.2 Add the `-- name: SnapshotsByVehicleBetween :many` query to `internal/telemetry/db/query.sql`, mirroring `SnapshotsByVehicleSince`'s explicit column list, index-reuse header comment, and `LIMIT 400`. Filter on `captured_at` (NOT `captured_date`): `WHERE account_id = @account_id AND tesla_id = @tesla_id AND captured_at >= @start_bound AND captured_at < @end_bound ORDER BY captured_at ASC LIMIT 400`. The header comment states the bounds' derivation (`start_bound = start + 1 calendar day`, `end_bound = end + 2 calendar days`, half-open) and the `EffectiveDate ∈ [start, end]` inclusive semantics, and notes why `captured_date` is NOT used (POLLER_TIMEZONE coupling). Params `@account_id uuid.UUID`, `@tesla_id int64`, `@start_bound pgtype.Timestamptz`, `@end_bound pgtype.Timestamptz`. Depends on: nothing (first wave, can run in parallel with 1.1).

## 2. Codegen

- [x] 2.1 Run `make sqlc` to regenerate `internal/telemetry/db/` (`query.sql.go`, `models.go` unchanged) so the new `SnapshotsByVehicleBetween` / `SnapshotsByVehicleBetweenParams` symbols exist in package `telemetrydb`. Confirm `git status` shows only the generated `db/` files changed (no migration touched). Depends on: 1.2 (query must exist in `query.sql`).

## 3. Implementation (reader + dbStore)

- [x] 3.1 Add a `snapshotsByVehicleBetween` method to the `store` seam interface in `internal/telemetry/service.go` (mirrors `snapshotsByVehicleSince`'s seam): `snapshotsByVehicleBetween(ctx context.Context, accountID uuid.UUID, teslaID int64, start, end time.Time) ([]Snapshot, error)`. Depends on: 1.1.
- [x] 3.2 Implement `dbStore.snapshotsByVehicleBetween` in `internal/telemetry/service.go`: translate `(start, end)` → `(startBound, endBound)` via `start.AddDate(0,0,1)` and `end.AddDate(0,0,2)` (calendar-day arithmetic, DST-safe — mirrors `rowToSnapshot`'s `EffectiveDate` `AddDate`), bind them as `pgtype.Timestamptz` via `timestamptzFrom`, call `d.q.SnapshotsByVehicleBetween`, map each row via the existing `rowToSnapshot` (no per-method duplication), return a non-nil empty slice on no rows (D5 parity with `snapshotsByVehicleSince`). Doc comment states the `+1day`/`+2day` translation honors `EffectiveDate ∈ [start, end]` (design D1/D5). Depends on: 2.1, 3.1.
- [x] 3.3 Implement `(r *reader) SnapshotsByVehicleBetween` in `internal/telemetry/reader.go`: thin pass-through to `r.store.snapshotsByVehicleBetween(ctx, accountID, teslaID, start, end)`, mirroring `SnapshotsByVehicleSince`'s pattern. The compile-time `var _ Reader = (*reader)(nil)` assertion pins the new interface method. Depends on: 2.1, 3.1, 3.2.

## 4. Offline unit tests (fake store — no DB)

- [x] 4.1 Extend the `fakeHistoryStore` (or add a sibling fake) in `internal/telemetry/reader_test.go` to capture `(gotAccount, gotTeslaID, gotStart, gotEnd)` and return configurable snapshots for `SnapshotsByVehicleBetween`. Write methods (insertSnapshot, insertPollAttempt, upsertSuperchargerSession) and `latestSnapshotsByAccount` / `snapshotsByVehicleSince` panic to catch accidental cross-path calls. Depends on: 3.3.
- [x] 4.2 Add an offline unit test asserting `reader.SnapshotsByVehicleBetween` passes `(accountID, teslaID, start, end)` through to the store unchanged (the `+1day`/`+2day` translation is the `dbStore` impl's job, not the `reader`'s) and returns the store's slice verbatim. Depends on: 4.1.

## 5. Integration tests (real Postgres via testcontainers — design boundary semantics)

- [x] 5.1 Add a `db_read_integration_test.go` case `TestReadStore_SnapshotsByVehicleBetween_EffectiveDateInRange`: seed snapshots across consecutive nights for one vehicle within one account; call `SnapshotsByVehicleBetween(ctx, accID, teslaID, start, end)` with a sub-window; assert every returned `Snapshot.EffectiveDate` calendar day ∈ `[start, end]` inclusive, ordered ascending by `EffectiveDate`, and `captured_at` matches the stored row. Depends on: 3.3.
- [x] 5.2 Add a case asserting `end` inclusivity at the boundary: a snapshot whose `EffectiveDate` calendar day == `end` is included; a snapshot whose `EffectiveDate` calendar day == `end+1day` is excluded. And `start` inclusivity: `EffectiveDate == start` included, `start-1day` excluded. Depends on: 5.1.
- [x] 5.3 Add a case asserting an **empty (non-nil)** slice and nil error when the window contains no snapshots for the vehicle. Depends on: 5.1.
- [x] 5.4 Add a case asserting **tenant isolation**: call `SnapshotsByVehicleBetween` for `(accountA, vehicleV)`; confirm snapshots belonging to `accountB` or to a sibling vehicle of `accountA` do NOT appear. Depends on: 5.1.

## 6. Verification

- [x] 6.1 Run `go test ./internal/telemetry/...` (and `make check` if available) and confirm the full telemetry suite is green (testcontainers Postgres auto-provisioned), including all new `Between` tests and the existing `Since`/`EffectiveDate` tests. No live Tesla call. Depends on: 5.4.
- [x] 6.2 Run `go vet ./...` and `go build ./...` repo-wide; confirm no other `telemetry.Reader` implementer broke (only `*reader` implements it today; grep callers). `SnapshotsByVehicleSince` callers continue to compile (additive). Depends on: 6.1.
- [x] 6.3 Confirm `git status` shows changes only under `internal/telemetry/` and `openspec/changes/RM8-telemetry-between-range-port/` — no migration file created, no other module touched, no HTML. Depends on: 6.2.