# RM57 — Re-key the Supercharger pair on tesla_id

Source ticket: MAG-67 — https://linear.app/magus-monitor/issue/MAG-67/4-re-key-the-supercharger-pair-on-tesla-id-reshape-the-mirror
Parent: MAG-63 (step 4 of 7). Blocker MAG-66 (the authorization seam) is done.
Generated by: `kkpa-dev-harness-pipeline`, from `kkpa-pickup-linear-ticket MAG-67`.

Unit tests: **included** — updated, not new. Tests that assert on `account_id` move to
`tesla_id`. The ticket says so directly.

## Why

Both Supercharger tables are keyed on `account_id`. That is wrong for data about a car.
`account.vehicles` already says which vehicles a user may see. This roadmap removes
`account_id` from both tables and keys them on `tesla_id`.

## The decision that sets the scope

`tesla_id` becomes `NOT NULL` on both tables.

Nothing except `account_id` ties a `telemetry.supercharger_history` row to an account.
`vin` only reaches an account through `account.vehicles`, and that lists **registered**
vehicles only. So once `account_id` is dropped, a row with `tesla_id IS NULL` — an
"orphan" — can never be read again by any query. Keeping the column nullable would not
save it. It would only store rows nobody can reach.

The owner chose `NOT NULL`. The consequence is a real behaviour change, not just a schema
change: `collectChargingHistory` currently writes `tesla_id = NULL` when a session's VIN
is not a registered vehicle (`internal/telemetry/service.go:348`). It must now skip that
session instead. Tesla's charging-history API still returns it, but we stop storing it.

This reverses what MAG-63 wrote for these two tables ("key on `vin`") and what
`20260906000002_add_mirror_watermarks.sql` says in its own comment about orphan recovery.
Both are corrected by this roadmap, not ignored.

Row counts checked before the choice: `telemetry.supercharger_history` has **0** rows with
`tesla_id IS NULL`, and `charging.supercharger_sessions` has **0**. The migration still
deletes such rows first, as a guard for any row that appears before it runs. On the
owner's database today it deletes nothing. The down-migration cannot bring a deleted row
back — it is gone.

## Tiers

Legend: `[ ]` pending (change not created) · `[~]` in progress (change exists, not archived) · `[x]` done (archived)

| Status | Change | Module | Scope | depends_on | Proposal prompt |
| --- | --- | --- | --- | --- | --- |
| `[ ]` | `RM57-telemetry-rekey-supercharger-history-on-tesla-id` | `telemetry` | Migration on `telemetry.supercharger_history`: delete `tesla_id IS NULL` rows, `SET NOT NULL`, `DROP COLUMN account_id`, replace the three `account_id` indexes. Domain type `SuperchargerHistory` drops `AccountID` and `TeslaID` becomes `int64`. Every `SuperchargerHistoryReader` method drops its `accountID` parameter. `collectChargingHistory` skips a session whose VIN is not a registered vehicle and counts it. Tests re-keyed. | — | Re-key `telemetry.supercharger_history` on `tesla_id`. Keep `UNIQUE (session_id)`. Replace `idx_supercharger_history_vehicle_time`, `idx_supercharger_history_account_time` and `idx_supercharger_history_account_updated` with only the indexes a query that exists today actually reads — justify each one. Drop `account_id`. Make `tesla_id NOT NULL`, deleting NULL rows first. Change the write path to skip a session whose VIN is not registered. |
| `[ ]` | `RM57-charging-rekey-supercharger-sessions-on-tesla-id` | `charging` | Two migrations. `charging.supercharger_sessions`: replace `UNIQUE (account_id, session_id)` with `UNIQUE (session_id)`, delete `tesla_id IS NULL` rows, `SET NOT NULL`, `DROP COLUMN account_id`, replace `idx_supercharger_sessions_vehicle_stop`. `charging.mirror_watermarks`: re-key the cursor from `account_id` to `tesla_id`. Ports `MirrorSessions`, `ListSessionsByVehicle*`, `VerifySession`, `MirrorWatermark`, `AdvanceMirrorWatermark` drop `accountID`. Tests re-keyed. | 1 | Re-key `charging.supercharger_sessions` and `charging.mirror_watermarks` on `tesla_id`. Read the current DDL first — the sessions table gained `status` and `inferred_capacity_kwh_calc` after its first migration, and the index is named `idx_supercharger_sessions_vehicle_stop` today, not `idx_charge_sessions_vehicle_stop`. Leave `charging.manual_charge_entries` alone; MAG-68 owns it. |
| `[ ]` | `RM57-gateway-authorize-supercharger-reads` | `gateway` | The Supercharger Stats page routes its per-vehicle reads through `authorizeVehicle`, which MAG-66 built and nothing calls yet. `internal/gateway/handlers/supercharger.go` (11 call sites), `handlers.go`, `gateway.go`. Leader-owned integration in the same tier: the mirror loop in `internal/app/processor.go` becomes one pass per `tesla_id`, plus the call-site updates in `internal/app/app.go`, `internal/analytics/reader.go` and `internal/analytics/recalculate.go`. | 1, 2 | Make the gateway prove vehicle ownership before every Supercharger read, using `authorizeVehicle`. Then rewrite the mirror in `internal/app/processor.go` to loop per `tesla_id` instead of per account, using the existing `SuperchargerHistoryByVehicleUpdatedSince` port. |

## Facts already checked — do not re-derive

- `telemetry.SuperchargerHistoryReader` already has
  `SuperchargerHistoryByVehicleUpdatedSince` (`internal/telemetry/telemetry.go:588`,
  `reader.go:193`). Tier 3 drops its `accountID` parameter. It does not need a new method.
- `authorizeVehicle` exists at `internal/gateway/handlers/handlers.go:1061` and has no
  caller. Tier 3 is its first user.
- The charging index is `idx_supercharger_sessions_vehicle_stop` today, renamed by
  `internal/charging/db/migrations/20260902000003_move_charging_to_own_schema.sql:32`.
  The telemetry index names in the ticket are correct.
- `internal/analytics` also reads the charging session ports (`reader.go`,
  `recalculate.go`). The ticket does not mention it. Tier 3 covers it.
- MAG-63's index rule binds every tier: a `UNIQUE (a, b)` constraint already builds a
  btree that serves equality on `a`, point lookups, range scans on `b` inside one `a`,
  and `ORDER BY b DESC`. Do not add an index that constraint already covers. Justify
  every index against a query that exists today.

## Out of scope

- `charging.manual_charge_entries` — MAG-68.
- `analytics.vehicle_metrics` and `vehicle_metric_watermarks` — MAG-69.
- The conventions, specs and KB sweep — MAG-70. This roadmap still updates the docs its
  own changes invalidate: `internal/telemetry/AGENTS.md`, `internal/charging/AGENTS.md`,
  `openspec/specs/telemetry/spec.md`, `openspec/specs/charging/spec.md`,
  `kkpa/context/architecture/telemetry-ingest-only.md`,
  `kkpa/context/workflows/supercharger-stats-read.md`.
- Dropping `telemetry.vehicle_snapshots.account_id` — MAG-76 owns that, and it is blocked
  by the migration-order problem.

## Done when

Neither Supercharger table has `account_id`. `tesla_id` is `NOT NULL` on both. The mirror
runs one pass per `tesla_id`. The gateway proves ownership before every Supercharger read.
