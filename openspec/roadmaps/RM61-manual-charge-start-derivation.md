# RM61 — Derive the start battery % on manual charges

Source ticket: MAG-40 — https://linear.app/magus-monitor/issue/MAG-40/recalculated-battery-start
Generated by: `kkpa-dev-harness-pipeline`, from `kkpa-pickup-linear-ticket MAG-40`.

Unit tests: **included** — the owner asked for them on 2026-09-15. New tests cover the
derivation wiring, the `start_battery_source` values, and the capacity-query exclusion.

## Why

`/supercharger-stats` already fills a missing start battery %. The user types the energy
and the end %, and the module computes the start % for them. `/external-charges` does not.
There, the start % is a required field the user must always type.

MAG-40 asks for the same help on the manual charges page.

The maths already exists and is shared. `derivedStartBatteryPct`
(`internal/charging/capacity.go:110`) computes `end - energy/capacity*100`. It rounds half
away from zero and returns nothing outside `[0, 100]` instead of clamping. Today only
`VerifySession` calls it. This roadmap gives `Writer.Create` and `Writer.Update` the same
call.

## What the ticket got wrong, and this roadmap corrects

MAG-40's REQ 1 asks to "replace `packCapacityKWh` to use the effective capacity of the
previous month". **That work is already done.** MAG-32 / RM52 delivered it.

| The ticket says | The truth |
|---|---|
| `packCapacityKWh` should be changed to read the MAG-32 capacity | It already does — `internal/charging/capacity.go:47` reads `charging.monthly_effective_capacity` through `LatestMeasuredCapacity`. |
| It should read "the previous month" | It reads the **newest non-NULL** row. The job runs on day 1 for the previous month (`internal/app/processor.go:397`), so the newest row *is* the previous month whenever that month had 3 or more samples. |
| (implied) the pages need changing for REQ 1 | The word "capacity" appears nowhere in `internal/gateway`. Neither page reads or shows a pack capacity. This is a write-path concern only. |

So **REQ 1 contributes no code**. The owner confirmed on 2026-09-15 to keep
`packCapacityKWh` exactly as it is. Newest-non-NULL is deliberately better than
previous-month-only: when a month is thin its value is NULL, and newest-non-NULL falls back
to the last real measurement, while previous-month-only would fall back to the hardcoded
`62.0`.

MAG-40 also says the start % "should be optional". Inside `internal/charging` it already
is — `Entry.StartBatteryPct` is a `*int` and `RequiredFieldsFor`
(`internal/charging/validation.go:25`) never demands it, for either status. **The gateway is
what forces it**: `internal/gateway/handlers/external_charges.go:1403` says
*"start_battery_pct stays UNCONDITIONALLY required"*, and both forms set `Required: true`.
That is tier 2's work, not a schema change.

## Facts already checked — do not re-derive

Read from the code on 2026-09-15.

- `packCapacityKWh` has exactly two callers: `resolveEnergy`
  (`internal/charging/service.go:301`) and `VerifySession`
  (`internal/charging/session_verifier.go:173`).
- `derivedStartBatteryPct` has exactly one caller today:
  `internal/charging/session_verifier.go:178`.
- `derivedEnergyKWh` has exactly one caller today: `internal/charging/service.go:305`.
- Hardcoded capacity values outside tests, exactly two: `defaultPackCapacityKWh = 62.0`
  (`internal/charging/capacity.go:13`), and a `car_type` → kWh map in
  `internal/analytics/capacity.go:15` (`model3: 75`, `modely: 75`, `models: 100`,
  `modelx: 100`), used by `analytics.Reader` at `internal/analytics/reader.go:158`.
- `manual_charge_entries` columns today: `id`, `created_by_account_id`, `tesla_id`, `vin`,
  `charged_on`, `energy_added_kwh`, `price`, `currency`, `started_at`, `ended_at`,
  `start_battery_pct`, `end_battery_pct`, `charging_type`, `location_kind`,
  `location_label`, `notes`, `created_at`, `updated_at`, `inferred_capacity_kwh_calc`,
  `status`, `energy_source`, `odometer_km`, `price_source`. **There is no
  `battery_pct_source` column.**
- `manual_charge_entries.status` CHECK is `IN ('IN_PROGRESS','DONE')`
  (`20260829000002_add_entry_status.sql:33`). `supercharger_sessions.status` CHECK is
  `IN ('IN_PROGRESS','DONE_CALCULATED','DONE')` (`20260903000004_add_session_status.sql:40`).
- **The two status columns are not the same kind of thing.** On `supercharger_sessions`
  status is module-computed and no caller may supply it. On `manual_charge_entries` it is a
  user-picked `<select>` — `external_charge_create_form.templ:86` and
  `external_charge_row_edit.templ:81`.
- `manual_charge_entries` has exactly three writers, all SQL CRUD driven by the user's own
  request: `query.sql:27` (INSERT), `:89` (UPDATE), `:118` (DELETE). No background job
  writes this table.
- `/external-charges` has only two i18n keys today — `charges_page.title` and
  `charges_page.back_to_dashboard` (`internal/gateway/i18n/catalog.go:189`).
- The create form already knows how to show a suggestion as a placeholder rather than a
  value: `StartBatteryPctSuggestion` (`external_charges_vm.go:197`), rendered as
  `placeholder` at `external_charge_create_form.templ:115`.

## The circular-feedback problem this roadmap must not create

`ListValidManualEntryCapacitiesForPeriod` (`internal/charging/db/query.sql:426`) feeds
`monthly_effective_capacity` from manual entries where `energy_source = 'USER'` **and**
`inferred_capacity_kwh_calc IS NOT NULL`.

`inferred_capacity_kwh_calc` is a generated column: `energy / (delta/100)`. If we derive
the start % **from** a capacity, that generated column comes back equal to the capacity we
divided by. The row's `energy_source` stays `USER`, because the user really did type the
energy. So the row would pass both filters and feed the constant back into itself.

The Supercharger side already avoids exactly this, with the `status = 'DONE'` filter at
`query.sql:447` that excludes `DONE_CALCULATED`. The manual table has no equivalent marker.
Tier 1 adds one.

## Decisions settled with the owner before any artifact was written

Captured in the Step 2 interview on 2026-09-15.

| # | Decision | Why |
|---|---|---|
| RD1 | REQ 1 changes no code. `packCapacityKWh` and `LatestMeasuredCapacity` stay exactly as they are. | The work was already delivered by MAG-32 / RM52, and newest-non-NULL survives a thin month while previous-month-only would fall back to the hardcoded 62.0. |
| RD2 | A new `start_battery_source` column on `manual_charge_entries`, values `USER` / `ESTIMATED`, module-computed and never accepted from a caller. The capacity query adds `AND start_battery_source = 'USER'`. | This table already carries two module-computed provenance columns in that exact shape — `energy_source` and `price_source` — so it is the vocabulary an agent already finds here. Reusing `status` was rejected: unlike the Supercharger table, status here is a user-picked dropdown, so a module-owned value would either appear as a choice or silently overwrite what the user selected. |
| RD3 | The edit form renders a derived start % as a **placeholder**, not as a filled value. `Update` then sees an empty start % and derives it again. | `Writer.Update` requires every mutable field on every call, so a pre-filled derived value would come back looking user-supplied and get stamped `USER` — feeding the capacity job the exact loop RD2 exists to stop. The form already has this placeholder pattern in `StartBatteryPctSuggestion`. |
| RD4 | A start % the user actually typed is never recomputed or overwritten. | Same rule the Supercharger path already states and keeps. A person's own reading must never be replaced by a computed guess. |
| RD5 | The "optional" text goes in the **field help under the input**, not in a page-level card. ES: "Opcional. Déjalo vacío y lo calculamos con la energía y el % final." EN: "Optional. Leave it empty and we calculate it from the energy and the end %." | The user reads it where they are typing. A card at the top of the page is read once and then ignored. |
| RD6 | The `internal/analytics` 62-vs-75 disagreement is **out of scope** and goes to the backlog. | Analytics may not read another module's table, so it needs a new public port out of `charging`. That is its own change, and it would move every efficiency number already shown. |

Three more points were settled by **reading the code**, not by asking, and are recorded here
so no tier re-opens them:

- **No precedence rule is needed** between the two derivations. `resolveEnergy` runs only
  when energy is nil; the new rule runs only when the start % is nil. Each needs the other's
  input to be present, so they can never both fire.
- **No `FOR UPDATE` lock is needed.** `VerifySession` locks because the nightly mirror
  rewrites `supercharger_sessions.energy_kwh` underneath it. `manual_charge_entries` has no
  background writer, and the energy arrives in the same request that writes the row, so
  there is no stored value to read first.
- **Status is unaffected.** `start_battery_pct` is not in `RequiredFieldsFor`, and
  `promoteIfComplete` never looks at it. A derived start % cannot move a row between
  `IN_PROGRESS` and `DONE`.

## Tiers

Status legend: `[ ]` pending (change not created) · `[~]` in progress (change exists, not
archived) · `[x]` done (archived).

| Status | Change | Module | Scope | depends_on | Proposal prompt |
| --- | --- | --- | --- | --- | --- |
| `[ ]` | `RM61-charging-add-manual-entry-start-derivation` | `charging` | One migration on `charging.manual_charge_entries`: add `start_battery_source TEXT` with `CHECK (start_battery_source IN ('USER','ESTIMATED'))`, nullable exactly where `start_battery_pct` is nullable (a NULL start % has no provenance), and backfill every existing row that has a `start_battery_pct` to `'USER'` — every such value was typed by a person, because no derivation existed before this change. `query.sql`: add the column to `CreateEntry` and `UpdateEntry`, return it on every read, and add `AND start_battery_source = 'USER'` to `ListValidManualEntryCapacitiesForPeriod`. `sqlc generate`. In `capacity.go`, no change to `derivedStartBatteryPct`, `derivedEnergyKWh` or `packCapacityKWh` — they are called, not edited. In `service.go`, a new `resolveStartBatteryPct` sibling to `resolveEnergy`: when `e.StartBatteryPct` is nil and `e.EndBatteryPct` and the resolved energy are both non-nil, call `derivedStartBatteryPct` and return `ESTIMATED`; otherwise return the caller's value and `USER`. Wire it into both `Create` and `Update`, after `resolveEnergy` (the energy it divides by may itself have just been resolved). Add `StartBatterySource` to `Entry` with the same "ALWAYS COMPUTED BY THIS MODULE" doc contract `EnergySource` and `PriceSource` already carry, and ignore any caller-supplied value. Unit tests for `resolveStartBatteryPct` across every (energy, start, end) combination, and an integration test that a derived row is excluded from the capacity query. `internal/charging/AGENTS.md` and `openspec/specs/charging/spec.md`. | — | Give `Writer.Create` and `Writer.Update` the start-% derivation the Supercharger path already has. Call the existing `derivedStartBatteryPct` — do not write new maths, and do not touch `packCapacityKWh`. The migration adds `start_battery_source`; justify its index plan (almost certainly no index — `ListValidManualEntryCapacitiesForPeriod` runs once a month and already scans a period range). The one thing that must not happen: a row whose start % was derived must never reach `monthly_effective_capacity`, or the capacity constant feeds itself. |
| `[ ]` | `RM61-gateway-relax-start-battery-pct-required` | `gateway` | Make `start_battery_pct` optional on `/external-charges`. Remove `Required: true` from the input in `external_charge_create_form.templ:112` and `external_charge_row_edit.templ`, and remove the unconditional required check at `handlers/external_charges.go:1403-1412` (the range check at `:1416` stays — an empty value is allowed, a bad one is not). RD3: when a row's `start_battery_source` is `ESTIMATED`, the edit form renders the stored start % as a `placeholder`, not as a `Value`, so `Update` receives it empty and re-derives it; when it is `USER`, the value is filled as it is today. This needs `StartBatterySource` on the row view model in `external_charges_vm.go`. RD5: one new bilingual i18n key for the field help under the input — ES "Opcional. Déjalo vacío y lo calculamos con la energía y el % final.", EN "Optional. Leave it empty and we calculate it from the energy and the end %." Both languages non-empty, and `make i18n-guard` must pass. Update the tests that assert the field is required. | 1 | Stop forcing `start_battery_pct` on the manual charges page, and show a derived value as a placeholder rather than a filled input. Read `handlers/external_charges.go:1403` first — its own comment states the current rule, and that comment must be replaced with the new one. Do not re-open the derivation itself; tier 1 owns it. |

## Done when

A user can add or edit a manual charge, leave the start % empty, give the energy and the
end %, and the module fills the start % in. The row is marked `ESTIMATED`. That row never
reaches `monthly_effective_capacity`. A start % the user typed is still stored untouched
and still counts as capacity evidence.

## Future work

Recorded in `openspec/roadmaps/backlog.md` — do not leave it only here:

- **`analytics` — one pack capacity, not two.** `internal/analytics/capacity.go` keeps its
  own `car_type` → kWh map (75 for a Model 3) while `internal/charging` measures the real
  capacity per vehicle and falls back to 62.0. The two modules disagree about the same car.
  See backlog item 31.
