# Sync proposal — gateway

> Staged by `kkpa-context-curate from-spec`. This is a **draft** of KB edits derived from one
> approved OpenSpec capability spec. Review/edit the blocks below, then run
> `/kkpa-context-curate apply-sync` to write them into the real KB. Nothing here touches the
> canonical KB until applied. This file is self-contained — it embeds the proposed content, so it
> stays valid even after the OpenSpec change folder is archived/moved.

Target guide: `input-port/analytics/vehicle-stats.md`
Source spec:  `openspec/specs/gateway/spec.md` — Requirement: Vehicle Stats Page Shows A Gasoline Cost-Parity Tile (the one requirement added by RM68-gateway-add-km-per-gallon-tile)
Generated:    `2026-09-25`
Status: APPLIED 2026-09-25

Scope note: the gateway spec is 4,025 lines and covers many pages. Only the requirement this
change added is proposed here. The target guide already carries the eligibility rule, the
component map rows and the page-structure row (written by the change itself), so this delta adds
only the three rules the guide does not yet state, plus the routing aliases.

---

## [guide] ## Gotchas — APPEND

- **The gasoline price is never shown.** Only the km-per-gallon figure appears on the page, in
  either language. Do not add the price as a tile description or tooltip.
  _Source: spec gateway — Requirement: Vehicle Stats Page Shows A Gasoline Cost-Parity Tile._
- **The Km-per-gallon tile has no trend.** It carries no arrow and no period-over-period
  comparison, and the previous-period roll-up reads no prices. Adding a trend means a second
  `PricesForMonths` read per render.
  _Source: spec gateway — Requirement: Vehicle Stats Page Shows A Gasoline Cost-Parity Tile._
- **A failed price read hides only this tile.** The handler logs the error and continues. The
  other seven figures still render from their own reads; the page does not show its error state.
  _Source: spec gateway — Requirement: Vehicle Stats Page Shows A Gasoline Cost-Parity Tile._

## [index] ## Input ports — pages & endpoints — ADD ROWS

| `Km per gallon tile` / `gasoline equivalent` | the gasoline cost-parity tile on `/vehicle-stats` | `analytics` | `input-port/analytics/vehicle-stats.md` |
| `Km por galón` (tile label, ES) | the gasoline cost-parity tile on `/vehicle-stats` | `analytics` | `input-port/analytics/vehicle-stats.md` |
