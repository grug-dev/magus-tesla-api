# Sync proposal — gateway

> Staged by `kkpa-context-curate from-spec`. This is a **draft** of KB edits derived from one
> approved OpenSpec capability spec. Review/edit the blocks below, then run
> `/kkpa-context-curate apply-sync` to write them into the real KB. Nothing here touches the
> canonical KB until applied. This file is self-contained — it embeds the proposed content, so it
> stays valid even after the OpenSpec change folder is archived/moved.

Target guide: `use-case/charging/verify-session-battery.md`
Source spec:  `openspec/specs/gateway/spec.md`
Generated:    2026-09-14
Status: APPLIED 2026-09-14

---

## Why this proposal is small

The gateway spec's RM57 delta is **already recorded** in
`architecture/gateway-reader-writer-ports.md`. Its write-aperture table names `authorizeVehicle`
over the session-selected vehicle, and its prose already covers the `HTTP 404`, the single
`RegisteredVehicles` read, and the fact that a session on another vehicle of the *same* account
is no longer writable unless that vehicle is selected.

One gap is left. `INDEX.md` routes `session battery edit`, `verify session battery` and
`battery percentage correction` to `use-case/charging/verify-session-battery.md` — **not** to the
architecture guide. So someone asking about this flow lands on the use-case file and never sees
the behaviour change. The blocks below close that, in the file the router actually reaches.

---

## [guide] ## Input / output — APPEND

- **Which session is writable now depends on the session-selected vehicle.** The write is scoped
  to the vehicle held in the user's session, not to the account. A session belonging to another
  of the account's own vehicles is not writable through this route until that vehicle is
  selected. Selecting it, then repeating the save, scopes the write to it.
  _Source: spec gateway — Requirement: Supercharger Stats page._
- **No vehicle selected means `HTTP 404`, and `VerifySession` is never called.** This happens on
  a stale session whose selection was cleared, or on a hand-crafted `PATCH` sent without first
  loading a page that selects a vehicle. It is the same status an unresolvable vehicle already
  returned, not a new one.
  _Source: spec gateway — Requirement: Supercharger Stats page._

## [guide] ## Conventions & gotchas — APPEND

- **The handler reads the vehicle from the session, never from `resolveSelectedVehicle`.**
  `resolveSelectedVehicle` auto-picks a vehicle when the session holds none. On a write that
  would silently save to a car the user never chose. Reading `currentVehicle` and rejecting an
  empty selection is what makes the 404 above correct rather than a lost edit.
  _Source: spec gateway — Requirement: Supercharger Stats page, Scenario: A request with no
  vehicle selected in the session is rejected._
- **Ownership is proved once, not twice.** The check reuses the same `RegisteredVehicles` read
  the route already performs. Do not add a `resolveSelectedVehicle` call in front of
  `authorizeVehicle` — that costs a second account query on every save and proves nothing new.
  _Source: spec gateway — Requirement: Supercharger Stats page._

## [index] ## Use cases — ADD ROWS

| `why can't I edit this session` | the write is scoped to the session-selected vehicle; select that vehicle first | `charging` | `use-case/charging/verify-session-battery.md` |

---

## Reviewer notes — not applied, for your judgement

**Check for overlap before applying.** `architecture/gateway-reader-writer-ports.md` already
states the same-account-other-vehicle rule at roughly line 105. This proposal repeats it in the
use-case guide **on purpose**, because that is where `INDEX.md` sends the reader. If you would
rather keep it in one place, drop the first `## Input / output` bullet and instead add a link
from the use-case guide to the architecture one.

**Same stale-pointer pattern as the charging proposal.** This guide's existing conventions cite
`(design D4/D5)` and `(design D9)`, which point into archived docs. Out of scope here; `MAG-70`
is the natural home.
