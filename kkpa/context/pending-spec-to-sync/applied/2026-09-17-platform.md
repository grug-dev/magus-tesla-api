# Sync proposal — platform

> Staged by `kkpa-context-curate from-spec`. This is a **draft** of KB edits derived from one
> approved OpenSpec capability spec. Review/edit the blocks below, then run
> `/kkpa-context-curate apply-sync` to write them into the real KB. Nothing here touches the
> canonical KB until applied. This file is self-contained — it embeds the proposed content, so it
> stays valid even after the OpenSpec change folder is archived/moved.

Target guide: `architecture/deploy-log-files.md`
Source spec:  `openspec/specs/platform/spec.md`
Generated:    2026-09-17
Status: APPLIED 2026-09-17

---

<!--
Scope note. The `platform` capability spec holds 13 requirements. Twelve already have KB
coverage (`architecture/deployment-stack.md`, `architecture/nightly-cycle.md`, and the tz-guard
material). This proposal is the delta for the two requirements the change
`platform-add-named-deploy-logs` added: "Named And Located Deploy Logs" and "Time-Bounded Named
Log Retention".

That change also wrote `architecture/deploy-log-files.md` by hand, so the guide's prose is
already correct. What it lacks is the `_Source: spec platform — Requirement: ..._` citation on
each rule. Those citations are what lets a later reader tell a project rule from an author's
preference, so the blocks below add them.

No `## Glossary` block: the guide's glossary line is already right.
No `## Component map` block: spec.md carries no file paths.
No `## How maintenance works` block: the guide's behaviour prose already matches the spec.
-->

## [guide] ## Conventions & gotchas — APPEND

- **`web`, `poller` and `caddy` must each keep a named log file at a fixed host path.** An
  operator has to reach a service's log without first looking up a per-container id. `db` and
  `migrate` are exempt and stay on the container runtime's own log command.
  _Source: spec platform — Requirement: Named And Located Deploy Logs._
- **Named log data must be purged automatically past its retention period.** Without it the files
  grow until the host disk fills. Retention today is 14 days.
  _Source: spec platform — Requirement: Time-Bounded Named Log Retention._
- **A fast-growing log must rotate on size, not only on the daily check.** A noisy failure loop
  can fill a file long before the next scheduled run, so the size threshold has to fire on its
  own.
  _Source: spec platform — Requirement: Time-Bounded Named Log Retention._
- **Exactly one rotation mechanism may govern a log file.** A file that rotates itself is never
  also handled by the host mechanism. Two rotators on one file lose data.
  _Source: spec platform — Requirement: Time-Bounded Named Log Retention._

## [index] ## Architecture topics — ADD ROWS

| `deploy logs` (named, rotated log files for `web`, `poller`, and `caddy` on the VPS, and the host `logrotate` job) | `architecture/deploy-log-files.md` |
| `magus-logs` | the `~/magus-logs` host folder → `architecture/deploy-log-files.md` |
| `log rotation` | the host `logrotate` job for `web.log`/`poller.log`, and Caddy's own rotation for `caddy.log` → `architecture/deploy-log-files.md` |
| `logrotate` | synonym of `log rotation` → `architecture/deploy-log-files.md` |
