// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1

package referencedb

import (
	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

// One gasoline price per calendar month, in COP. Belongs to no vehicle and no user -- it is an external reference value, read to turn charging cost into a km-per-gallon comparison. Every row is entered by hand, in its own migration, never by a running process. Owned exclusively by internal/reference; no other module reads this table directly.
type FuelPrice struct {
	ID uuid.UUID
	// First day of the calendar month this price applies to. CHECK-enforced to always be a month start. Always written literally by the migration that seeds it -- there is no runtime writer for this table.
	Period pgtype.Date
	// The currency price is expressed in. COP today and for the foreseeable future. No DEFAULT: every row is written by an explicit migration that always names it.
	Currency string
	// Price of one gallon of regular gasoline, in currency. NUMERIC(14,2), never a unit suffix -- this is the platform monetary exemption, paired with currency instead of a suffix.
	Price pgtype.Numeric
	// Set once, on the row's first INSERT. This table has no upsert path: every row is written once, by its own seeding migration, and never updated.
	CreatedAt pgtype.Timestamptz
	// Present for uniformity with every sibling table. No writer in this module ever changes an existing row, so it equals created_at for the life of a row.
	UpdatedAt pgtype.Timestamptz
}
