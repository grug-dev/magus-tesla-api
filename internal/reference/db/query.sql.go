// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: query.sql

package referencedb

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const pricesForMonths = `-- name: PricesForMonths :many
SELECT
    gs.period::date AS period,
    fp.currency,
    fp.price
FROM generate_series(
    date_trunc('month', $1::date),
    date_trunc('month', $2::date),
    interval '1 month'
) AS gs(period)
CROSS JOIN LATERAL (
    SELECT currency, price
    FROM reference.fuel_prices
    WHERE period <= gs.period::date
    ORDER BY period DESC
    LIMIT 1
) AS fp
ORDER BY gs.period
`

type PricesForMonthsParams struct {
	StartPeriod pgtype.Date
	EndPeriod   pgtype.Date
}

type PricesForMonthsRow struct {
	Period   pgtype.Date
	Currency string
	Price    pgtype.Numeric
}

// Returns one row per month in [@start_period, @end_period] that resolves to
// a stored price: the month's own row when one exists, otherwise the newest
// row at or before it. generate_series walks every first-of-month between
// the two (already month-truncated) bounds; the LATERAL subquery behaves as
// an inner join, so a month with nothing to fall back to -- every stored row
// is later than it -- drops out of the result instead of returning NULLs.
// Both bounds are month-truncated here, in SQL, mirroring
// charging.CapacityForMonth's and analytics.MonthlyMetricsBetween's identical
// choice to keep month normalization out of Go and away from make tz-guard's
// reach.
func (q *Queries) PricesForMonths(ctx context.Context, arg PricesForMonthsParams) ([]PricesForMonthsRow, error) {
	rows, err := q.db.Query(ctx, pricesForMonths, arg.StartPeriod, arg.EndPeriod)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []PricesForMonthsRow
	for rows.Next() {
		var i PricesForMonthsRow
		if err := rows.Scan(&i.Period, &i.Currency, &i.Price); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
